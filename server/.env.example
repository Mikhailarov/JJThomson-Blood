# Copy to .env and edit values for MySQL if you want to use MySQL
# If you don't set these, the app will use a local sqlite DB at server/data/dev.db
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=blood_bank
PORT=3000
