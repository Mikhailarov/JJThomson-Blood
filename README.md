
```markdown
# 🩸 เว็บไซต์ประชาสัมพันธ์ธนาคารเลือด (Blood Bank PR Website)

ระบบประชาสัมพันธ์และจัดการข้อมูลธนาคารเลือด เพื่ออำนวยความสะดวกในการจัดเก็บข้อมูลสต็อกเลือด การลงทะเบียนผู้บริจาค และประชาสัมพันธ์ข่าวสารกิจกรรมต่างๆ

---

## 👥 คณะผู้พัฒนา (Development Team)

| รายชื่อ-นามสกุล | รหัสนิสิต | บทบาทหน้าที่ |
| :--- | :---: | :--- |
| **นายกิตติศักดิ์ ภูมิยิ่ง** | 67021444 | Front End Developer |
| **นายจารุวัฒน์ ยะถา** | 67021488 | Back End Developer |
| **นายพรรษนนท์ สุยาละ** | 67021961 | Front End Developer |
| **นายระพีพงศ์ สุทา** | 67022131 | Back End Developer |
| **นายระพีพัชร จันต๊ะ** | 67022142 | Front End Developer |

---

## 🚀 การติดตั้งและเริ่มใช้งาน (Getting Started)

### 1. ติดตั้ง Dependencies
เปิด Terminal ใน VS Code แล้วรันคำสั่ง:
```bash
npm install

```

### 2. ตั้งค่า Environment Variables

สร้างไฟล์ `.env` ขึ้นมา (หรือ Copy จาก `.env.example`) และกำหนดค่าดังนี้:

```ini
PORT=3000
SESSION_SECRET=your_random_secret_key
DB_HOST=localhost
DB_USER=root
DB_PASS=password  # รหัสผ่าน MySQL ของคุณ
DB_NAME=blood_bank

```

### 3. การเตรียมฐานข้อมูล (Database Import) 🗄️

ต้องทำการ Import ไฟล์ฐานข้อมูลเพื่อให้ระบบมีโครงสร้างตารางและข้อมูลตัวอย่าง:

1. เปิดโปรแกรม **HeidiSQL**
2. เชื่อมต่อกับ Session ของคุณ (**Host:** `localhost`, **User:** `root`, **Port:** `3306`)
3. คลิกขวาที่ชื่อ Connection เลือก **Create new > Database** ตั้งชื่อว่า `blood_bank`
4. ไปที่เมนู **File > Load SQL file...** แล้วเลือกไฟล์ `.sql` จากโฟลเดอร์โปรเจกต์
5. กดปุ่ม **Execute (F9)** เพื่อทำการรันสคริปต์สร้างตารางและข้อมูล

### 4. เริ่มรัน Server

```bash
npm run start

```

> เมื่อสำเร็จจะขึ้นข้อความ: `Server running on http://localhost:3000`

---

## 👤 บัญชีสำหรับทดสอบ (Test Accounts)

*รหัสผ่านทุกบัญชีคือ: `123456*`

| Role | Email | Password | Role ID |
| --- | --- | --- | --- |
| 👑 **Admin** | `admin@gmail.com` | 123456 | 1 |
| 👤 **User** | `kittisak@gmail.com` | 123456 | 2 |
| 👤 **User** | `jaruwat@gmail.com` | 123456 | 2 |

---

## 📡 การทดสอบ API (API Endpoints)

สามารถทดสอบผ่าน **Postman** หรือ **Thunder Client**:

* **Login:** `POST /api/login`
* *JSON Body:* `{ "email": "admin@gmail.com", "password": "123456" }`


* **Check Status:** `GET /api/health`
* **Logout:** `POST /api/logout`

---

## 🛠️ สคริปต์ช่วยจัดการระบบ (Helper Scripts)

| คำสั่ง | รายละเอียด |
| --- | --- |
| `node server/scripts/inspect_users.js` | 👀 ดูรายชื่อผู้ใช้ใน SQLite |
| `node server/scripts/inspect_mysql_users.js` | 👀 ดูรายชื่อผู้ใช้ใน MySQL |
| `node server/scripts/set_admin_password.js` | 🔑 รีเซ็ตรหัสผ่าน Admin เป็น `123456` |
| `node server/scripts/migrate_passwords.js` | 🔐 เข้ารหัส Password ด้วย Bcrypt |

---

## 🗄️ ข้อมูลการเชื่อมต่อฐานข้อมูล

* **Hostname/IP:** `localhost` หรือ `127.0.0.1`
* **Username:** `root`
* **Port:** `3306`

```
